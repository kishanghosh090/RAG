[project]
name = "langgraph-krish"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "kishanghosh090", email = "kishanghosh090@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "langchain>=1.3.17",
    "langgraph>=1.2.11",
    "langsmith>=0.11.1",
]

[project.scripts]
langgraph-krish = "langgraph_krish:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
